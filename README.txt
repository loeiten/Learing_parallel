Learing_parallel
================

IF MPI ONLY ASSIGNS ONE RANK TO ALL PROCESSORS:
- Most probably you have messed up your installation of mpi
- Solution: Delete all mpi files (f.ex through software manager)
-           sudo apt-get install mpich2 libmpich2-dev

Files for learning OpenMPI
